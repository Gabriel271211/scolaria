{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "scolaria",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import os,http.server,socketserver; os.chdir('C:/Users/silva/OneDrive/Bureau/scolaria'); port=int(os.environ.get('PORT',8000)); httpd=socketserver.TCPServer(('',port),http.server.SimpleHTTPRequestHandler); httpd.serve_forever()"],
      "autoPort": true
    }
  ]
}
